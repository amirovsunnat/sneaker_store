import 'package:flutter/material.dart';

class GetStarted4Screen extends StatelessWidget {
  const GetStarted4Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Color(0xFF1483C2),),
    );
  }
}